nelmio_api_doc:
    documentation:
        info:
            title: StudyOn Billing API
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        default:
            path_patterns:
                - ^/api/v1(?!/doc(?:\.json)?$)
